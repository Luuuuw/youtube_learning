# MiniMax API Key（必填，初翻/查词依赖）
MINIMAX_API_KEY=your_minimax_api_key_here

# DeepSeek API Key（必填，审校 + 补译漏条；缺失则降级为纯 MiniMax 模式）
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 初始管理员密码（可选）
# 不设置则首次启动生成 16 位随机密码到 data/.initial-admin-password.txt
# Render 上首次部署建议设一个，部署成功后删除该环境变量
# INITIAL_ADMIN_PASSWORD=

# 视频 CDN base URL（Phase 1 上线 R2 后必填）
# 例：https://pub-xxx.r2.dev 或自定义域 https://videos.yourdomain.com
# 留空则视频从本地 /public/content/ 加载（开发模式）
# NEXT_PUBLIC_VIDEO_CDN_BASE=
<<<<<<< Updated upstream
=======

# 翻译自动二审开关（可选，默认开启）
# 翻译完成后自动跑 DeepSeek 二审 + 重译标红段，省去手动开 /dashboard/translation-review
# 设为 0 关闭（节省 API 调用，但翻译质量需手动审核）
# AUTO_REVIEW_TRANSLATION=1

# SiliconFlow API Key（可选，做字幕空洞 ASR 补漏用）
# 注册 https://cloud.siliconflow.cn 拿 key（SenseVoiceSmall 模型免费）
# 缺失时翻译审看板的「自动补漏」按钮会报错，其他功能不受影响
# SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxx

# AI safe-mutation HITL bypass（默认 0=启用 HITL；紧急情况 admin 手动 1=跳过审批直接 apply）
# 慎用。仅在 prompt 经过充分验证、明确不会误改时打开。
# 即便 bypass，备份 + audit log 仍工作，所以仍可 revert。
# AI_HITL_BYPASS=0
>>>>>>> Stashed changes
